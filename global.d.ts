// global.d.ts

// Remove the custom global declarations unless you have a very specific reason.
// If you need to extend types, use module augmentation instead.

// This export is needed to make this a module
export {};
